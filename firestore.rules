rules_version = '2';

// Assignment / demo only: fully open access. Anyone with your Firebase web config / API access
// can read or write any document. Do not ship or host real user data with this.

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
